import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.benalf.app',
  appName: 'RegistAppAlf_Ben',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
